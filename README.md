Huh. Never used a README before.

# Overview

This was made for a school project. **Crisis.py** is the main file. It reads from the *question.json* file. It can be easily created and modified by running **question_maker.py**.

# Dependencies

You need to install python3 to run the console and the question making tool. Moreover, the console requires **tkinter**
```pip install tkinter```